## What's new in v1.0.4

**Bug fixes**
- Fixed: "Already running" dialog appeared on reboot when Windows restart-app recovery relaunched the app alongside the startup entry (second instance now silently exits)
- Fixed: deleting a group with children crashed with "Null or empty path" ArgumentException
- Fixed: delete-group confirmation dialog appeared behind Explorer window instead of on top

**Improvements**
- Scrollbar thumb is now narrower for a cleaner look
